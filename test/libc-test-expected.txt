# SPDX-FileCopyrightText: (C) 2026 Gavin John
# SPDX-License-Identifier: GPL-3.0-or-later
#
# test/libc-test-expected.txt -- the adjudication ledger for
# third_party/libc-test, read by tools/libc-test.sh.
#
# One or more profile rules per corpus test:
#
#     <name>  <profile selector>  <disposition>  <reason>
#
# The dispositions are PASS, BUG, UNIMPL, NA and FLAKY.  Every non-PASS
# row requires a reason.  Compiler/process observations remain separate:
# pedantic requires BUG to build and fail and UNIMPL not to build; strict
# performs those probes and then rejects either disposition.
#
# A disposition may be overridden by adding a more specific profile rule.
# Profile dimensions include runtime, target and host architecture, WOW64,
# kernel32 support and named runtime capabilities. The shared resolver
# rejects ambiguous matches and profiles for which no rule exists. This
# baseline deliberately claims only the configuration in which it was measured;
# another platform must be annotated before it can use the ledger.
#
# This file does NOT need one row per real case. It lists only the cases
# whose disposition deviates from PASS, plus one trailing wildcard row
# (case "*") that declares PASS as this corpus's own default for every
# case with no more specific row. tools/testlib.py's resolve() falls back
# to a case="*" rule whenever a case-specific search comes up empty, so
# "everything else is PASS" is a real, matched rule here, not a hidden
# default. Which cases are "real" -- so a case with no row here still gets
# adjudicated, rather than silently dropped -- comes independently from
# tools/libc-test.sh's own glob over
# third_party/libc-test/src/{functional,regression}/*.c (see its
# read_ledger()), not from what happens to be listed below. This mirrors
# test/posix-opts-expected.txt's own wildcard row exactly.
#
# Baseline: spicule 661c577, libc-test 68edb8b, x86_64-win32-tcc, under
# the project's patched Wine (which implements RtlCloneUserProcess, so
# nothing here is Wine-fork-blocked; on stock Wine several of these
# become NA at run time instead, by design).
#
# This environment has a working x86_64-win32-tcc but no runnable x86_64
# Wine; unless a row says otherwise, its non-PASS reason rests on
# compile/link evidence (and, where noted, a native aarch64-linux run of
# the unmodified upstream source), not an observed Wine run.
#
# COUNTS PASS=114 BUG=4 UNIMPL=6 NA=21 FLAKY=1
clocale_mbfuncs	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: this library's only encoding is UTF-8 (MB_CUR_MAX=4), but the test opens setlocale(LC_CTYPE, "C") and requires the single-byte-C-locale invariant MB_CUR_MAX==1.
crypt	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: this library implements only POSIX-mandatory DES crypt(3); every test case uses a glibc/BSD extension hash format ($1$/$2a$/$2x$/$2y$/$5$/$6$), which correctly fails with EINVAL.
daemon-failure	runtime=wine,target_arch=x86_64,kernel32=no	NA	The test needs the shim's t_fdfill() to exhaust the fd table so daemon()'s open("/dev/null") fails with EMFILE, but t_fdfill() is stubbed (no enforced RLIMIT_NOFILE), so daemon() succeeds instead.
dlopen	runtime=wine,target_arch=x86_64,kernel32=no	NA	dlopen() correctly reports the missing module, but this harness only builds each corpus source as its own standalone executable, so dlopen_dso.so is never produced for this test to open (see dlopen_dso's own row).
dlopen_dso	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	tcc: error: unresolved reference to 'main'
execle-env	runtime=wine,target_arch=x86_64,kernel32=no	NA	no /bin/sh on the target; execle("/bin/sh",...) cannot succeed
fcntl	runtime=wine,target_arch=x86_64,kernel32=no	BUG	F_SETLK across fork(): the child's lock request succeeds against a range the parent holds, when POSIX requires a conflict. Real NT/Wine lock code is process-keyed, not file-object-keyed, so this is suspected to be an identity bug in the patched Wine fork's RtlCloneUserProcess clone handshake, not spicule's fork()/fcntl().
malloc-brk-fail	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: malloc() uses the pre-existing NT process heap (src/malloc/nt/plat_malloc.c), whose already-reserved arena can still satisfy allocation after address-space exhaustion, unlike the musl/brk-specific premise this test assumes.
malloc-oom	runtime=wine,target_arch=x86_64,kernel32=no	NA	t_setrlim(RLIMIT_DATA, 0) is stubbed (setrlimit() not enforced), so the NT process heap's existing reserve is never exhausted and the malloc-OOM premise is never established.
mbc	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: wchar_t is 16-bit here (the Windows ABI), so a non-BMP character decodes as a UTF-16 surrogate pair rather than the single wchar_t this test expects.
popen	runtime=wine,target_arch=x86_64,kernel32=no	NA	no /bin/sh on the target -- the shell script reaches cmd.exe, which rejects it (external-suites.md, build assumption 3)
pthread_atfork-errno-clobber	runtime=wine,target_arch=x86_64,kernel32=no	NA	t_setrlim exits 77 because setrlimit() is not enforced on this target, so the errno-preservation premise cannot be exercised
pthread_create-oom	runtime=wine,target_arch=x86_64,kernel32=no	NA	t_setrlim(RLIMIT_DATA, 0) is not enforced, so the NT process heap's existing reserve is never exhausted and the pthread_create-OOM premise is never established.
pthread_exit-dtor	runtime=wine,target_arch=x86_64,kernel32=no	FLAKY	Patched Wine's concurrent RtlCloneUserProcess path can stall under load: 40/40 serial passes but 2 timeouts in 160 runs at 20-way concurrency, with no destructor assertion failure observed.
pthread_robust	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: pthread_mutex_destroy() returns EBUSY for any locked mutex, which POSIX's ERRORS section sanctions, but this test (a musl-specific choice, not a POSIX requirement) expects destroying a self-held robust mutex mid-EOWNERDEAD-recovery to succeed.
setenv-oom	runtime=wine,target_arch=x86_64,kernel32=no	NA	t_setrlim(RLIMIT_DATA, 0) is not enforced, so the NT process heap's existing reserve is never exhausted and setenv() succeeding is not an OOM defect.
sigprocmask-internal	runtime=wine,target_arch=x86_64,kernel32=no	NA	Design difference: __libc_current_sigrtmin() is 35 here, so signals 32-34 are not reserved and are blockable.
socket	runtime=wine,target_arch=x86_64,kernel32=no	BUG	Builds and links clean under x86_64-win32-tcc (SOCK_NONBLOCK now supported) but fails at runtime under the project's patched Wine in real CI (rc=1); the prior PASS rested on compile/link evidence alone (no Wine in that sandbox), and the specific failing assertion is not yet triaged.
spawn	runtime=wine,target_arch=x86_64,kernel32=no	NA	posix_spawnp() spawns "echo", which has no NT executable image (echo is a cmd.exe builtin, not on PATH), so it correctly answers ENOENT; spicule's own posix_spawnp() coverage (test/posix-spawn.c) resolves a name that does exist.
sscanf_long	runtime=wine,target_arch=x86_64,kernel32=no	NA	Unverifiable in this environment: the test needs an enforced RLIMIT_STACK, but t_setrlim is stubbed.
syscall-sign-extend	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	syscall-sign-extend.c:7: error: include file 'sys/syscall.h' not found
tgmath	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	tgmath.c:28 uses `0.5+0*I`, but I is undefined under this tcc (no _Complex support); tgmath's real-only float/double/long-double dispatch works, only the complex half of this case fails.
tls_align	runtime=wine,target_arch=x86_64,kernel32=no	BUG	Builds and links clean now that tls_align_dso.c is linked in (extra_srcs_for()) but fails at runtime under the project's patched Wine in real CI (rc=1); the prior PASS rested on compile/link evidence alone (no Wine in that sandbox), and which of the four TLS descriptor checks fails is not yet triaged.
tls_align_dlopen	runtime=wine,target_arch=x86_64,kernel32=no	NA	dlopen() correctly reports the nonexistent path, but this harness only builds each corpus source as its own standalone executable, so tls_align_dso.so is never produced for this test to open (see tls_align_dso's own row).
tls_align_dso	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	tcc: error: unresolved reference to 'main'
tls_get_new-dtv	runtime=wine,target_arch=x86_64,kernel32=no	NA	dlopen() correctly reports the exhausted search, but this harness only builds each corpus source as its own standalone executable, so tls_get_new-dtv_dso.so is never produced for this test to open (see tls_get_new-dtv_dso's own row).
tls_get_new-dtv_dso	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	tcc: error: unresolved reference to 'main'
tls_init_dlopen	runtime=wine,target_arch=x86_64,kernel32=no	NA	dlopen() correctly reports the nonexistent path, but this harness only builds each corpus source as its own standalone executable, so tls_init_dso.so is never produced for this test to open (see tls_init_dso's own row).
tls_init_dso	runtime=wine,target_arch=x86_64,kernel32=no	UNIMPL	tcc: error: unresolved reference to 'main'
tls_local_exec	runtime=wine,target_arch=x86_64,kernel32=no	BUG	Over-aligned TLS addresses fail: pinned tcc writes IMAGE_TLS_DIRECTORY.Characteristics=0, so the PE loader is never told the required 4096-byte alignment.
vfork	runtime=wine,target_arch=x86_64,kernel32=no	NA	no /bin/sh on the target; the second half execl()s "/bin/sh -c"
wordexp	runtime=wine,target_arch=x86_64,kernel32=no	NA	The supported subset (IFS splitting, ${x:-d}/${x-d}/${x:+y}) passes; the remaining cases assume a full external POSIX shell environment (e.g. echo/printf images) this NT target does not provide.
*	*	PASS	no more specific rule matched; suite defaults to PASS
